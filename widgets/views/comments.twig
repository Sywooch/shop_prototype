{% if comments %}
    <ul>
        {% for comment in comments %}
            <li>
                {{ comment.name }}
                </br>{{ comment.date }}
                </br>{{ comment.text }}
            </li>
        {% endfor %}
    </ul>
{% endif %}
