
<form name="search" action="{{ url.to({0:'/search'}) }}" method="GET">
    <input type="text" name="{{ app.params.searchKey }}" size="40" value="{{ app.request.get(app.params.searchKey) }}">
    <input type="submit" value="Search">
</form>
