
{{ use('yii/widgets/ActiveForm') }}

{% set form=active_form_begin({'id':'search-form', 'action':url.to({0:'/search'}), 'method':'POST'}) %}
    {{ form.field(searchModel, 'text').textInput({'size':40, 'value':app.request.get('search')}).label(false) | raw }}
    <input type="submit" value="Search">
{{ active_form_end() }}
