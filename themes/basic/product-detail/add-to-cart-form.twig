{{ use('yii/widgets/ActiveForm') }}

{% set form=active_form_begin({'id':'add-to-cart-form', 'action':url.to({0:'/cart/set'}), 'method':'POST'}) %}
    {{ form.field(purchasesModel, 'quantity').input('number', {'step':1, 'min':1}) | raw }}
    {{ form.field(purchasesModel, 'id_color').dropDownList(colorsMapList) | raw }}
    {{ form.field(purchasesModel, 'id_size').dropDownList(sizesMapList) | raw }}
    {{ form.field(purchasesModel, 'id_product').hiddenInput({'value':productsModel.id}).label(false) | raw }}
    {{ form.field(productsModel, 'price').hiddenInput({'value':productsModel.price}).label(false) | raw }}
    <input type="submit" value="{{ yii.t('base', 'Add to cart') }}">
{{ active_form_end() }}
