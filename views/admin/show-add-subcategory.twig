
{{ register_asset_bundle('app/assets/GetSubcategoryWithDisableAsset') }}

{% include '@app/views/parts/user-buttons.twig' %}

{% include '@app/views/parts/admin-menu.twig' %}

{{ use('yii/widgets/ActiveForm') }}

<p>Здесь вы можете управлять товарными подкатегориями</p>

{% if subcategoryList %}
    <p><strong>Текущие подкатегории</strong></p>
    <ul>
        {% for subcategoryModel in subcategoryList %}
            <li>
                {{ subcategoryModel.name }} ({{ subcategoryModel.categories.name }})<br>
                <a href="{{ url.to({0:'admin/update-subcategory', 'id':subcategoryModel.id}) }}">Обновить подкатегорию</a><br>
                <a href="{{ url.to({0:'admin/delete-subcategory', 'id':subcategoryModel.id}) }}">Удалить подкатегорию</a>
            </li>
        {% endfor %}
    </ul>
{% else %}
    <p>Похоже, мы пока не можем предложить эту штуку!</p>
{% endif %}

{% include '@app/views/parts/filters-admin-subcategory.twig' %}

{% include '@app/views/parts/add-subcategory-form.twig' %}





