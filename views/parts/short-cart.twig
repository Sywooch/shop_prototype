
<p>Bag: {{ app.cart.getTotalProducts() | default(0) }}, Total cost: {{ (app.cart.getTotalCost() * app.shopUser.currency.exchange_rate) | number_format(2, '.', ' ') }} {{ app.shopUser.currency.currency }} 

{% if app.cart.getProductsArray() %}
    <a href="{{ url.to({0:'shopping-cart/index'}) }}">Go to cart</a>
    {% include '@app/views/parts/clean-cart-form.twig' %}
{% endif %}
