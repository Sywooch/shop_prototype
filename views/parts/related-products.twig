
<p><strong>Связанные продукты</strong></p>

<ol>
{% for product in objectsProducts.related %}
    <li>
        {% if product.date %}
            {{ app.formatter.asDate(product.date, 'dd.MM.yyyy') }}<br>
        {% endif %}
        <strong><a href="{{ url.to({0:'product-detail/index', 'categories': product.categories, 'subcategory': product.subcategory, 'id': product.id}) }}">{{ product.name }}</a></strong><br>
        {{ (product.price * app.shopUser.currency.exchange_rate) | number_format(2, '.', ' ') }} {{ app.shopUser.currency.currency }}<br>
    </li>
{% endfor %}
</ol>
